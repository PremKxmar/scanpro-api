# =============================================================================
# ScanPro API - Minimal Requirements for Deployment
# =============================================================================
# This file contains only the dependencies needed for the API server.
# For full development, use requirements-full.txt

# Core
opencv-python-headless>=4.8.0   # Headless version for servers
numpy>=1.24.0
scipy>=1.11.0

# Deep Learning (CPU only for deployment)
torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0                     # For MobileNetV3 encoder

# API Server
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.0.0

# PDF Generation
reportlab>=4.0.0
Pillow>=10.0.0
